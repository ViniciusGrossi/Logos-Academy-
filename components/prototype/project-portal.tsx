"use client";
import Link from "next/link";
import { ArrowRight, FolderKanban } from "lucide-react";
import type { ProjectDetail, ProjectSummary } from "@/specs/api.contracts";
import { useLiveApi } from "./live-api";
import { EmptyState, ErrorState, LoadingState } from "./state-lab";
export function ProjectPortal(){const {data,error,loading,reload}=useLiveApi<readonly ProjectSummary[]>("/api/student/projects");const portfolio=useLiveApi<{items:readonly ProjectDetail[]}>("/api/student/portfolio");if(loading||portfolio.loading)return <LoadingState/>;if(error||portfolio.error)return <ErrorState retry={()=>{void reload();void portfolio.reload();}} message={error?.message??portfolio.error?.message??"Erro"}/>;const projects=data??[];const approved=portfolio.data?.items??[];if(!projects.length)return <EmptyState scope="projeto iniciado"/>;return <><header className="student-intro"><div><span className="meta-label">Portfólio privado</span><h1>Projetos em construção.</h1></div><p>Cada projeto só avança com evidências e feedback pedagógico.</p></header><section className="operation-panel"><div className="section-heading"><div><span className="meta-label">Trilha ativa</span><h2>Seus projetos</h2></div></div><div className="evidence-list">{projects.map(project=><Link className="evidence-row" key={project.id} href={`/projetos/${project.id}`}><FolderKanban/><div><strong>{project.title}</strong><small>{project.completedActivityCount}/{project.activityCount} atividades · {project.status}</small></div><span className="version-marker">ciclo {project.cyclePosition}</span><ArrowRight/></Link>)}</div></section><section className="operation-panel"><div className="section-heading"><div><span className="meta-label">Evidências aprovadas</span><h2>Seu portfólio</h2></div></div>{approved.length?<div className="evidence-list">{approved.map(project=><Link className="evidence-row" key={project.id} href={`/projetos/${project.id}`}><FolderKanban/><div><strong>{project.title}</strong><small>{project.activities.length} evidência(s) preservada(s)</small></div><ArrowRight/></Link>)}</div>:<p>Quando um projeto for aprovado, ele aparecerá aqui.</p>}</section></>}
